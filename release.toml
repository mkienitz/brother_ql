allow-branch = ["main"]
tag-message = "chore: release {{crate_name}} version {{version}}"
pre-release-commit-message = "chore: Release {{crate_name}} version {{version}}"
pre-release-hook = ["bash", "../../scripts/update-changelog.sh"]
publish = false
push = false
