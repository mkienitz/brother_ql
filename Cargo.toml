[package]
name = "brother_ql"
version = "1.0.4"
description = "Generate Brother QL Raster Command data from images"
edition = "2021"
license = "MIT"
authors = ["Maximilian Kienitz <contact@kienitz.dev>"]
repository = "https://github.com/mkienitz/brother_ql"
exclude = [
	"flake.nix",
	"flake.lock",
	".envrc",
]

[dependencies]
image = "0.24.8"
itertools = { version = "0.12.0", features = ["use_alloc"] }
thiserror = "1.0.56"
